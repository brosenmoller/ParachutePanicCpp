#pragma once
#include <string>

const std::string GAME_NAME = "Speed Racer";
const int SCREEN_WIDTH = 800;
const int SCREEN_HEIGHT = 600;

const int FRAME_RATE = 60;
const float FRAME_TIME = 1 / FRAME_RATE;

const int FRICTION_COEFFICIENT = 1;
